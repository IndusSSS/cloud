
###############################################################################
#  TRAEFIK (reverse-proxy + Let’s Encrypt)                                     #
###############################################################################
services:
  traefik:
    image: traefik:v2.9
    restart: unless-stopped
    command:
      - --configFile=/etc/traefik/traefik.yml        # dynamic/static merged
    ports:
      - "80:80"                                      # :80  → web
      - "443:443"                                    # :443 → websecure
    volumes:
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./acme.json:/acme.json
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      - "traefik.enable=true"
    networks:
      - web

###############################################################################
#  PHP-FPM  + NGINX  (upload.php)                                              #
###############################################################################
  php-fpm:
    build:
      context: ./backend
    restart: unless-stopped
    volumes:
      - ./backend:/var/www/html                # your PHP code
    depends_on:
      - db
    # Traefik does not need to reach PHP-FPM directly
    labels:
      - "traefik.enable=false"

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ./backend:/var/www/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php-fpm
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.upload.rule=Host(`cloud.smartsecurity.solutions`) && Path(`/upload.php`)"
      - "traefik.http.routers.upload.entrypoints=websecure"
      - "traefik.http.routers.upload.tls.certresolver=le"
      - "traefik.http.services.upload.loadbalancer.server.port=80"
    networks:
      - web           # nginx must be reachable by Traefik

###############################################################################
#  DASHBOARD  (Vue + Vite build, served by Nginx)                              #
###############################################################################
  dashboard:
    build:
      context: ../dashboard/admin-dashboard
    image: admin-dashboard:latest
    container_name: admin-dashboard
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard.rule=Host(`admin.smartsecurity.solutions`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.tls.certresolver=le"
      - "traefik.http.services.dashboard.loadbalancer.server.port=80"
    networks:
      - web

###############################################################################
#  FASTAPI API                                                                 #
###############################################################################

  api:
    build:
      context: ../services/api
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "8000"
    networks:
      - web
    environment:
      DATABASE_URL: "postgresql://ssc:…@db:5432/sensordb"
    labels:
      - "traefik.enable=true"

      # incoming on cloud.smartsecurity.solutions/api…
      - "traefik.http.routers.api.rule=Host(`cloud.smartsecurity.solutions`) && PathPrefix(`/api`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls.certresolver=le"

      # mirror under admin.smartsecurity.solutions/api…
      - "traefik.http.routers.api-admin.rule=Host(`admin.smartsecurity.solutions`) && PathPrefix(`/api`)"
      - "traefik.http.routers.api-admin.entrypoints=websecure"
      - "traefik.http.routers.api-admin.tls.certresolver=le"

      # point at your Uvicorn port
      - "traefik.http.services.api.loadbalancer.server.port=8000"

      # strip off the `/api` before sending to FastAPI
      - "traefik.http.middlewares.api-strip-prefix.stripprefix.prefixes=/api"
      - "traefik.http.routers.api.middlewares=api-strip-prefix"
      - "traefik.http.routers.api-admin.middlewares=api-strip-prefix"


##########################################################################
#  WEBSOCKET  SERVICE                                                          #
###############################################################################
  ws:
    build: ../services/ws
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ws.rule=Host(`cloud.smartsecurity.solutions`) && PathPrefix(`/ws`)"
      - "traefik.http.routers.ws.entrypoints=websecure"
      - "traefik.http.routers.ws.tls.certresolver=le"
      - "traefik.http.services.ws.loadbalancer.server.port=8000"
      # admin mirror
      - "traefik.http.routers.ws-admin.rule=Host(`admin.smartsecurity.solutions`) && PathPrefix(`/ws`)"
      - "traefik.http.routers.ws-admin.entrypoints=websecure"
      - "traefik.http.routers.ws-admin.tls.certresolver=le"
      - "traefik.http.routers.ws-admin.middlewares=ws-strip-prefix"
      - "traefik.http.middlewares.ws-strip-prefix.stripprefix.prefixes=/ws"
    networks:
      - web

###############################################################################
#  BACK-END SUPPORT                                                            #
###############################################################################
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ssc
      POSTGRES_PASSWORD: ChangeMeToAStrongPass!
      POSTGRES_DB: sensordb
    volumes:
      - db_data:/var/lib/postgresql/data
    # Private; not exposed via Traefik

  mqtt:
    image: eclipse-mosquitto:2.0
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
      - ./mosquitto/config/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    labels:
      - "traefik.enable=false"

  bridge:
    build: ../services/bridge
    restart: unless-stopped
    environment:
      DATABASE_URL: "postgresql://ssc:ChangeMeToAStrongPass!@db:5432/sensordb"
      MQTT_BROKER: mqtt
      MQTT_PORT: "1883"
      MQTT_TOPIC: sensors/+/data
    labels:
      - "traefik.enable=false"

###############################################################################
#  VOLUMES & NETWORKS                                                          #
###############################################################################
volumes:
  db_data:
  mosquitto_data:
  mosquitto_log:

networks:
  web:
    external: true        # pre-created:  docker network create web
